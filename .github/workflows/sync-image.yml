name: sync-image

on:
  workflow_dispatch:
    inputs:
      date:
        description: "可选：附加版本日期（例如 20251016），将额外同步 tag-<date>"
        required: false
        default: ""

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    outputs:
      toolchain-matrix: ${{ steps.generate.outputs.toolchain_matrix }}
      image-matrix: ${{ steps.generate.outputs.image_matrix }}
      has_ghcr: ${{ steps.check.outputs.has_ghcr }}
      has_docker: ${{ steps.check.outputs.has_docker }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Check available credentials
        id: check
        env:
          HAS_GHCR: ${{ secrets.GHCR_TOKEN != '' }}
          HAS_DOCKER: ${{ secrets.DOCKERHUB_TOKEN != '' }}
        run: |
          echo "has_ghcr=$HAS_GHCR" >> "$GITHUB_OUTPUT"
          echo "has_docker=$HAS_DOCKER" >> "$GITHUB_OUTPUT"
          echo "GHCR: $HAS_GHCR, Docker Hub: $HAS_DOCKER"

      - name: Generate matrix
        id: generate
        run: |
          toolchain_matrix=$(jq -c '{include:
            ([.matrix[].android] | unique | map({project: "ddk-toolchain", tag: .}))
          }' mapping.json)
          image_matrix=$(jq -c '{include:
            ([.android[] | {project: "ddk", tag: .name}] +
             [.android[] | {project: "ddk-min", tag: .name}])
          }' mapping.json)
          echo "toolchain_matrix=$toolchain_matrix" >> "$GITHUB_OUTPUT"
          echo "image_matrix=$image_matrix" >> "$GITHUB_OUTPUT"
          echo "Toolchain matrix: $toolchain_matrix"
          echo "Image matrix: $image_matrix"

  # Phase 1: CNB → GitHub
  sync-toolchain-to-github:
    name: cnb→github toolchain:${{ matrix.tag }}
    needs: prepare
    if: needs.prepare.outputs.has_ghcr == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.toolchain-matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup skopeo
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends skopeo jq

      - name: Login to GHCR
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          echo "$GHCR_TOKEN" | skopeo login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Sync images
        run: |
          build/sync-image.sh -s cnb -d github -p ${{ matrix.project }} -t ${{ matrix.tag }}
          if [ -n '${{ github.event.inputs.date }}' ]; then
            build/sync-image.sh -s cnb -d github -p ${{ matrix.project }} -t ${{ matrix.tag }} --new-date '${{ github.event.inputs.date }}'
          fi

  sync-image-to-github:
    name: cnb→github ${{ matrix.project }}:${{ matrix.tag }}
    needs: [prepare, sync-toolchain-to-github]
    if: needs.prepare.outputs.has_ghcr == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.image-matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup skopeo
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends skopeo jq

      - name: Login to GHCR
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          echo "$GHCR_TOKEN" | skopeo login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Sync images
        run: |
          build/sync-image.sh -s cnb -d github -p ${{ matrix.project }} -t ${{ matrix.tag }}
          if [ -n '${{ github.event.inputs.date }}' ]; then
            build/sync-image.sh -s cnb -d github -p ${{ matrix.project }} -t ${{ matrix.tag }} --new-date '${{ github.event.inputs.date }}'
          fi

  # Phase 2: GitHub → Docker Hub
  sync-toolchain-to-docker:
    name: github→docker toolchain:${{ matrix.tag }}
    needs: [prepare, sync-toolchain-to-github]
    if: needs.prepare.outputs.has_ghcr == 'true' && needs.prepare.outputs.has_docker == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.toolchain-matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup skopeo
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends skopeo jq

      - name: Login to GHCR
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          echo "$GHCR_TOKEN" | skopeo login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Login to Docker Hub
        env:
          DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASS: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "$DOCKERHUB_PASS" | skopeo login docker.io --username "$DOCKERHUB_USER" --password-stdin

      - name: Sync images
        run: |
          build/sync-image.sh -s github -d docker -p ${{ matrix.project }} -t ${{ matrix.tag }}
          if [ -n '${{ github.event.inputs.date }}' ]; then
            build/sync-image.sh -s github -d docker -p ${{ matrix.project }} -t ${{ matrix.tag }} --new-date '${{ github.event.inputs.date }}'
          fi

  sync-image-to-docker:
    name: github→docker ${{ matrix.project }}:${{ matrix.tag }}
    needs: [prepare, sync-image-to-github, sync-toolchain-to-docker]
    if: needs.prepare.outputs.has_ghcr == 'true' && needs.prepare.outputs.has_docker == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.image-matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup skopeo
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends skopeo jq

      - name: Login to GHCR
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: |
          echo "$GHCR_TOKEN" | skopeo login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Login to Docker Hub
        env:
          DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASS: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "$DOCKERHUB_PASS" | skopeo login docker.io --username "$DOCKERHUB_USER" --password-stdin

      - name: Sync images
        run: |
          build/sync-image.sh -s github -d docker -p ${{ matrix.project }} -t ${{ matrix.tag }}
          if [ -n '${{ github.event.inputs.date }}' ]; then
            build/sync-image.sh -s github -d docker -p ${{ matrix.project }} -t ${{ matrix.tag }} --new-date '${{ github.event.inputs.date }}'
          fi
